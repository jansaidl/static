zerops:
  - setup: static
    buildCommands:
    - echo "build commands - requirements.txt"
    - cat requirements.txt
    build:
      deployFiles: index.html
    addToRunPrepare:
        - requirements.txt  
    run:
      perapreCommands:
      - echo "prepare commands - requirements.txt"
      - cat requirements.txt
      initCommands:
      - echo "init commands - requirements.txt"
      - cat requirements.txt  
      base: static
